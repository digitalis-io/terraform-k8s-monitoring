# Minimal fixture — all Mimir module defaults (local-disk backend,
# create_namespace = true). Used to verify the namespace is created and named,
# and the Helm release points at the mimir-distributed chart.
mimir = {}
